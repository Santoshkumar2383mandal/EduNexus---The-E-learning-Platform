// const express = require('express');
// const router = express.Router();   

// const {capturePayment, verifySignature} = require('../controllers/Payments');
// const {autho, isStudent, isInstructor, isAdmin} = require('../middlewares/autho'); 
// const { route } = require('./User');

// router.post("/capturePayment",autho,isStudent,capturePayment);
// router.post("/verifySignature",autho,isStudent,verifySignature);

// module.exports = router;